# Workflow Overview — Acquisition Strategy Agent

Runnable case: `case_001_acquisition_strategy` with two separate views: `buyer_side` and `target_side`.

The same Apple → DarwinAI deal context is evaluated from two separate perspectives. Buyer-side and target-side inputs, evidence, ER/BRB, PCE, and outputs are intentionally separated to avoid mixing recommendation logic. Both views remain **Needs Human Review / Source replay pending**.
