# Market Configuration

Market adapters are configurable. HK is only the current adapter. New markets should define source hierarchy, field mapping, announcement types, and market-specific shell filter rules under `configs/markets/<market>/`.
