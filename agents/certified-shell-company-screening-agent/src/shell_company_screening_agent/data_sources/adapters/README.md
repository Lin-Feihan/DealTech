# Market adapters

Add new market adapters here or under a market-specific package. An adapter should provide source hierarchy, universe construction, issuer identifiers, announcement retrieval, and field mapping. HK is the current example adapter, not the framework boundary.
