from __future__ import annotations

from pathlib import Path


def extract_pdf_text_stub(path: Path) -> str:
    """Placeholder for public PDF parsing.

    Future implementation can use pdfplumber or pymupdf and must retain source
    document paths/URLs. v0.1 returns an empty string instead of guessing.
    """
    return ""
